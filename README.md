# DW3-DS-Android

El código fuente de la POC vive en `dw2003-dual-screen/`. La versión actual es **0.7.16-poc**.

APK de prueba: [DW2003-Dual-Screen-v0.7.16-poc-debug.apk](https://github.com/rsigristc/DW3-DS-Android/releases/tag/v0.7.16-poc).

Cada versión se fusiona en `main` y se publica con la etiqueta `vX.Y.Z-poc`. El workflow `Publish Android release` genera el APK y el `SHA256SUMS.txt`.
